# Relay project ignores

# build artifacts
dist/
build/
target/
*.exe~

# Go binaries (compiled outputs)
relayd/relayd
relayd/relayd.exe
station/station
station/station.exe
station/station-linux
station/nativec_linux

# Next.js / Node.js
.next/
node_modules/
# hide site from git so site folder won't be pushed
site/

relay-client/node_modules/
relayd/ui-src/node_modules

# Internal tooling
.superpowers/

# Root-level duplicate Go files (real code lives in relayd/ and station/)
/*.go
/*.test
relay.sln
relay_demo.py



# Runtime data (created by relayd at startup)
data/
*.db
*.log

# Local project config (written by relay init)
.relay.json

# Go workspace
go.work
go.work.sum

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
*.swp
